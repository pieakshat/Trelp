# Web

Next.js App Router application. Use TypeScript only.

Planned folders:

- `app/` — routes, layouts, Server Actions, and Route Handlers
- `components/` — application components
- `features/` — feature-local UI and logic
- `lib/` — server and integration utilities
- `stores/` — small Zustand stores for ephemeral UI state
